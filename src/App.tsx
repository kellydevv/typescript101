import styled from "styled-components";

function App() {
  return (
    <div>
      <form>
        <input type="text" placeholder="username" />
        <button>login</button>
      </form>
    </div>
  );
}

export default App;
